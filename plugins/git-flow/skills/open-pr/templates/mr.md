<!--
GitLab MR 템플릿 — git-flow:open-pr 스킬 내장 fallback.
리포에 .gitlab/merge_request_templates/*.md 가 있으면 그 템플릿이 우선하고 이 파일은 쓰지 않는다.
이슈당 1 MR 원칙 — 변경 유형이 둘 이상 섞이면 MR 을 분리한다.
아래 주석 플레이스홀더는 스킬이 실제 값으로 채운 뒤 MR 설명으로 제출한다 (빈 항목 / 안내 주석은 삭제).
-->

## 관련 이슈
<!-- 연결할 이슈가 있을 때만 아래 한 줄을 남긴다 (없으면 이 섹션째로 삭제 — placeholder 금지):
Closes #<이슈번호>       ← GitLab 이슈 (크로스 프로젝트는 group/project#42)
Closes MP-<번호>         ← Linear (연동된 저장소에서만 자동 전이)
-->

## 요약

<!-- 이 MR 을 짧은 한 문장으로 요약 -->

## 변경 유형

<!-- 아래 항목 중 하나만 선택한다 (택1). 두 유형 이상이 섞였다면 MR 을 분리한다. -->

- [ ] feat (기능 추가)
- [ ] fix (버그 픽스)
- [ ] refactor (동작 보존 구조 개선)
- [ ] chore (빌드·설정 등 기타 변경)
- [ ] ci (CI 파이프라인 변경)
- [ ] docs (문서 변경)
- [ ] test (테스트 코드만 변경)

## 변경 내용

<!-- 무엇을, 왜 바꿨는지. `<path>` — <한 줄 설명> 형태. 파일이 1개면 bullet 하나로 끝낸다. -->

- `<path>` —

## 테스트 / 검증

<!-- 실제로 돌린 검증만 적는다.
     버그픽스: 재현 테스트를 먼저 추가해 실패시키고 통과시켰는지.
     기능: 이슈의 완료 조건(AC)을 어떻게 확인했는지.
     문서·설정: 무엇으로 확인했는지 (예: `bash -n` 통과, 스킬 로드 확인). -->

- [ ]

## 체크리스트

- [ ] 연결할 이슈가 있으면 `Closes` 로 연결했다 (없으면 관련 이슈 섹션을 삭제 — placeholder 금지)
- [ ] 변경 유형을 하나만 선택했다 — 리팩토링을 기능 변경과 섞지 않았다
- [ ] 브랜치가 `<type>/<kebab-설명>` (이슈 키가 있으면 `<type>/MP-<번호>-<kebab-설명>`) 컨벤션이고, 보호 브랜치(`main`·`develop`)에 직접 커밋하지 않았다
- [ ] 모든 커밋에 `Created-By: Padosol` trailer 가 있다
- [ ] 셀프 리뷰 완료 — 변경 라인이 모두 이 이슈와 직결된다
- [ ] 테스트 / 검증 섹션에 실제 실행한 검증 근거를 적었다
