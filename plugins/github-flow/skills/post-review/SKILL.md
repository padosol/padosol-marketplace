---
name: post-review
description: |
  로컬에 저장된 코드 리뷰 보고서 (예: simplify-save 가 만든 docs/simplify/{파일}.md) 를 GitHub PR 코멘트로 게시해 사용자가 GitHub 에서 직접 머지 결정할 수 있게 한다. 본문에 padosol-review:{source} 형식 HTML 주석 마커를 박아 같은 PR 에서 재실행 시 새 코멘트를 달지 않고 기존 코멘트를 갱신한다 (gh api PATCH). Claude/Anthropic 브랜딩은 절대 추가하지 않고 `🤖 Generated by Padosol` 푸터만 사용. 사용자가 "리뷰 결과 PR 에 올려", "/post-review", "review 코멘트 달아", "GitHub 에 올려" 같은 표현을 쓰거나, simplify-save 직후 "PR 에 게시까지" 의도를 표현하면 트리거. 인자 없이 호출하면 현재 브랜치의 open PR + docs/simplify 의 가장 최근 보고서를 자동 결정.
---

# post-review

코드 리뷰 보고서를 GitHub PR 코멘트로 게시한다. 사용자가 PR 머지를 결정하려면 리뷰 결과를 GitHub 에서 볼 수 있어야 하는데, simplify-save 같은 스킬은 로컬 markdown 만 만들고 끝나기 때문에 그 결과를 PR 로 옮기는 단계가 따로 필요해 만든 스킬.

## 언제 트리거

- `/post-review` (인자 있거나 없거나)
- "리뷰 결과 PR 에 올려" / "review 코멘트 달아" / "GitHub 에 게시"
- `/simplify` 직후 사용자가 "PR 까지 올려" 의도 표현 → simplify-save 가 chain 호출

자동 트리거 금지: 사용자가 명시 의사 없으면 게시 안 함 (PR 코멘트는 외부 가시 액션).

## 인자

```
/post-review [보고서경로] [PR번호]
```

- 둘 다 생략 가능. 결정 우선순위는 섹션 1 참조.
- 명시 예: `/post-review docs/simplify/2026-05-07-pad-7.md 84`

## 절차

### 1. 대상 결정

#### PR 번호

1. 인자에 PR 번호 있으면 그대로
2. 없으면 현재 브랜치의 open PR:
   ```bash
   gh pr view --json number,state,headRefName --jq '.number'
   ```
3. 검색 실패 → 사용자에게 묻고 중단

#### 보고서 경로

1. 인자에 경로 있으면 그대로 (존재 검증 필수)
2. 없으면 `docs/simplify/` (또는 사용자가 simplify-save 에 넘긴 출력 디렉토리) 의 가장 최근 `.md` 파일:
   ```bash
   ls -t docs/simplify/*.md 2>/dev/null | head -1
   ```
3. 못 찾으면 사용자에게 simplify-save 먼저 실행하도록 안내 후 중단

### 2. 사전 검증

```bash
PR_INFO=$(gh pr view <PR#> --json number,state,headRefName)
```

- `state` 가 `OPEN` 이 아니면 자동 진행 금지. **`AskUserQuestion` 도구로 TUI 선택지 제시** — header `Non-OPEN PR`, options: `그래도 게시 (Recommended)` / `중단` / `다른 PR 지정`. 사용자가 "다른 PR 지정" 을 고르면 새 PR 번호를 자유 입력으로 받아 1단계부터 다시. plain text 질문 금지.
- 보고서 파일 size 가 0 이면 중단

### 3. 코멘트 본문 조립

마커 형식: `<!-- padosol-review:<source> -->` 첫 줄에 삽입. `<source>` 는 보고서가 어느 스킬 출신인지 (`simplify`, `audit`, ...). 디폴트 `simplify`.

본문 골격:

```markdown
<!-- padosol-review:simplify -->
### Code Review · Padosol simplify

> **Source:** `docs/simplify/<file>.md`
> **Posted:** <YYYY-MM-DD HH:MM UTC>
> [**Updated:** <YYYY-MM-DD HH:MM UTC> — 갱신 호출일 때만]

[**Issues:** <N> reuse · <M> quality · <K> efficiency · **Build:** <SUCCESS/FAILED>]

---

<보고서 markdown 본문 — 또는 장문 시 collapsible 감싸기 (아래 규칙 참고)>

---

<sub>🤖 Generated by Padosol</sub>
```

규칙:
- 마커는 **반드시 첫 줄**. idempotent update 의 anchor.
- 메타 banner (`> Source/Posted/Updated`) 는 blockquote 1개로 묶어 시각적 분리. Updated 라인은 4단계의 EXISTING 분기에서 update 일 때만 추가
- **Issues 한 줄 요약** 은 보고서 첫 줄/메타에 `**Issues:**` 가 있으면 추출해 banner 다음에 한 줄로 노출 (없으면 생략). 독자가 본문을 펼치기 전 신호로 활용
- Claude / Anthropic 브랜딩 (`🤖 Generated with Claude Code`, `Co-Authored-By: Claude ...`) 절대 금지. 푸터는 `🤖 Generated by Padosol` 만
- **장문 본문 처리** (보고서 본문 length > 8000 chars):
  ```markdown
  <details>
  <summary><strong>전체 리뷰 펼치기</strong> (<len> chars)</summary>

  <보고서 본문 그대로>

  </details>
  ```
  GitHub 코멘트는 길어지면 스크롤 피로가 큼. 8000 이하면 그대로 노출 (collapsible 불필요)
- 보고서 본문이 PR 코멘트 한도 (65536 chars) 를 넘으면 헤더 + Issues 요약 + "전체는 `<file>` 참조" 로 축약 (가드)

### 4. 기존 코멘트 검색 → update or create

```bash
EXISTING=$(gh api "repos/{owner}/{repo}/issues/<PR#>/comments" \
  --jq '[.[] | select(.body | startswith("<!-- padosol-review:simplify -->"))][0].id // empty')
```

분기:

#### 4-A. EXISTING 있으면 (update)

PATCH 직전에 본문 banner blockquote 에 `**Updated:** <YYYY-MM-DD HH:MM UTC>` 라인을 추가해 재조립한 뒤 호출. 독자가 PR 타임라인 알림 없이도 코멘트가 갱신됐음을 인지할 수 있도록.

```bash
gh api -X PATCH "repos/{owner}/{repo}/issues/comments/$EXISTING" \
  -F body=@/tmp/post-review-body.md
```

`gh pr comment` 자체는 update 미지원 → `gh api` PATCH 직접 호출.

#### 4-B. EXISTING 없으면 (create)

```bash
gh pr comment <PR#> --body-file /tmp/post-review-body.md
```

### 5. 결과 보고

생성/갱신된 코멘트 URL 캡처해 사용자에게 안내:

```
PR #<번호> 에 리뷰 코멘트 <create|update>:
  https://github.com/<owner>/<repo>/pull/<번호>#issuecomment-<id>

본문 출처: docs/simplify/<file>.md
```

## 멱등성 (idempotency)

같은 PR 에서 재실행 → 마커로 기존 코멘트 찾아 갱신. 신규 코멘트 누적 금지. 다른 source (예: `audit`) 는 다른 마커이므로 별도 코멘트.

마커 변경 시 기존 코멘트는 고아가 됨 — 그래서 마커는 정해진 형식 그대로 유지하고 source 만 갈아낄 것.

## 실패 모드 / 주의

- ❌ Claude 브랜딩 푸터 추가 — 프로젝트 컨벤션 위반
  ✅ `🤖 Generated by Padosol` 만
- ❌ MERGED 또는 CLOSED PR 에 무단 코멘트 — 의미 없거나 노이즈
  ✅ state 검증, 비-OPEN 이면 사용자 확인 후에만
- ❌ 마커 누락 — 다음 호출이 중복 코멘트 생성
  ✅ 첫 줄 마커 필수, 검증
- ❌ `gh pr comment --edit-last` 사용 — "본인의 마지막 코멘트" 만 갱신, 다른 코멘트가 사이에 끼면 엉뚱한 걸 덮어씀
  ✅ 마커 검색 + `gh api PATCH` 로 명시적 ID 갱신
- ❌ 보고서 본문을 그대로 셸 인자에 인라인 — 따옴표/백틱 escape 사고
  ✅ 항상 `--body-file` 또는 `-F body=@<파일>`

## 사용 예

### 예시 1: simplify 직후 chain

```
사용자: /simplify
Claude: [simplify-save] → docs/simplify/2026-05-07-pr-84-review.md 생성
사용자: 이거 PR 에도 올려줘
Claude: [/post-review 호출, 인자 없음]
        → 현재 브랜치 PR #84 자동 검색
        → docs/simplify 최신 파일 자동 선택
        → state=OPEN 검증
        → 마커 검색: 없음 → 신규 코멘트
        → URL 보고
```

### 예시 2: 명시 호출 + 재실행 (update)

```
사용자: /post-review docs/simplify/2026-05-07-pad-7.md 84
Claude: → 마커 검색: 있음 (코멘트 id=1234567)
        → gh api PATCH 로 갱신
        → URL 보고 (같은 코멘트 id, 갱신됨)
```

## See Also

- `simplify-save:simplify-save` — 게시 대상 보고서 생성 (이 스킬의 입력원)
- `/open-pr` (이 플러그인) — PR 자체를 만드는 스킬. post-review 는 **PR 이 이미 있는** 상황에서 동작
- GitHub Issues comment API: https://docs.github.com/en/rest/issues/comments
